# Local development with Docker Compose.
# Copy this file to .env and provide the Groq key if LLM features are required.

MONGO_URI=mongodb://mongo:27017/nexTalent
GROQ_API_KEY=
AUTONOMOUS_AGENT_VERBOSE=true

# Empty values are resolved by Docker Compose to http://localhost:8787.
VITE_API_URL=
